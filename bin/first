#!/usr/bin/env bash
echo '=========    SHOW HOME DIR'
ls -l ~
echo '=========    SHOW HOME fetch_compiled_dir DIR'
ls -l ~/fetch_compiled_dir
echo '=========    CREATE first_compiled in home'
echo "Some text first compiled  here." > ~/first_compiled
echo '=========    SHOW HOME DIR again'
ls -l ~/
