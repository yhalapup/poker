plan:
- first
- second

steps:
  fetch:
    script: bin/fetch

  first:
    script: bin/first
    output:
      - scm_artifact.tar
      - fetch_compiled

  second:
    script: bin/second
    input:
      - scm_artifact.tar
      - fetch_compiled
