plan:
- first
- second

#working_directory: /home/abc

steps:
  fetch:
    script: bin/fetch

  first:
    script: bin/first
    output:
      - scm_artifact.tar
      - fetch_compiled

  second:
    #working_directory: /home/abc_second
    script: bin/second
    input:
      - scm_artifact.tar
      - fetch_compiled
    output:
    - fetch_compiled_second


    collect:
      repo:
      - solano.yml
